import java.io.IOException;

/**
 * The driver for the Graph Algorithms program
 */
public class Driver {
    /**
     * Main methos which just throws an IO if the EdgeComputation read it doesnt word
     * @param args
     * @throws IOException if file read in does not work
     */
    public static void main(String[] args) throws IOException {
            EdgeComputation edgeComputation = new EdgeComputation(); //creates new edge computation object
    }

}
